[project]
name = "protein-lab-plugin"
version = "0.3.0"
description = "Local Codex plugin scripts and tests for Protein Lab."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
